# libs/pyproject.toml
[project]
name = "puma_pymcprotocol"          # ← 任意。pip 本家と衝突しない名前に
version = "0.0.1"
description = "Vendored pymcprotocol for PuMa project"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
